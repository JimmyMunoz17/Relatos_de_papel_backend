CREATE TABLE IF NOT EXISTS compras (
    id_compra BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    libro_id BIGINT NOT NULL,
    cantidad BIGINT CHECK (cantidad > 0),
    fecha_compra TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- Insertar con fechas explícitas
INSERT INTO compras (libro_id, cantidad, fecha_compra)
VALUES (1, 2, CURRENT_TIMESTAMP);

INSERT INTO compras (libro_id, cantidad, fecha_compra)
VALUES (2, 1, CURRENT_TIMESTAMP);

INSERT INTO compras (libro_id, cantidad, fecha_compra)
VALUES (3, 3, CURRENT_TIMESTAMP);